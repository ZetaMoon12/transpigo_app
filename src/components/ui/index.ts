/**
 * Exportaciones de componentes de la interfaz de usuario (UI)
 * Agrega aquí componentes reutilizables y compartidos (Botones, Inputs, Modales, etc.)
 *
 * Ejemplo:
 * export { Button } from './Button';
 * export { Input } from './Input';
 * export { Modal } from './Modal';
 * export { Spinner } from './Spinner';
 */
export { Stepper } from './stepper';

